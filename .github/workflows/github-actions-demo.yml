name: GitHub Actions | Linting and testing
run-name: ${{ github.actor }} is run linter and tests 🚀
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm ci
          npm test
          make test
